// how i should store my dates and years on the data base
// use unix time so it is easier to sort
// Have a year be its own object than within the object have a keys for each month and have each of those months have an array of objects containing the dates in order with there values.
